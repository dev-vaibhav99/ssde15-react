import React, { useContext } from 'react'
// import { CounterContext } from '../../../App'

const ComponentB = () => {

    // const counter = useContext(CounterContext)

    return (
        <div>
            {/* <h4>Component B:: Counter : {counter?.state.firstCounter}</h4>
            <button onClick={() => counter?.dispatch({ type: "increment", value: 1 })}>Increment</button>
            <button onClick={() => counter?.dispatch({ type: "decrement", value: 1 })}>Decrement</button>
            <button onClick={() => counter?.dispatch({ type: "increment5", value: 5 })}>Increment 5</button>
            <button onClick={() => counter?.dispatch({ type: "decrement5", value: 5 })}>Decrement 5</button> */}
        </div>
    )
}

export default ComponentB